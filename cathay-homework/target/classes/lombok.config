#lombok configuration:必須放在 src/main/java 文件夾下的目錄中(也可以放在實體同級的目錄下)
#stopBubbling = true, to tell lombok this is your root directory.
config.stopBubbling=true
#lombok will generate calls to the superclass implementation of hashCode and equals if your class extends something.
lombok.equalsAndHashCode.callSuper=call